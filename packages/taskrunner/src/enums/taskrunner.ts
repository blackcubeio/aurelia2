export enum TaskState {
    INITIALIZED = 'initialized',
    RUNNING = 'running',
    FINISHED = 'finished',
    FAILED = 'failed',
    STOPPED = 'stopped'
}
export enum RunnerState {
    AVAILABLE = 'available',
    RESERVED = 'reserved',
    RUNNING = 'running',
    FINISHED = 'finished',
    FAILED = 'failed',
    STOPPED = 'stopped'
}